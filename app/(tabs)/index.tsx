import {
  View, Text, TextInput, TouchableOpacity, ScrollView, SafeAreaView,
} from "react-native";

export default function Account()
{
  return(
    <View>
      <Text>
        INdex
      </Text>
    </View>
  );
}